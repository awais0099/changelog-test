# Feature: System-Aware Dark Mode Toggle

Implemented global design variables to support seamless switching 
between light and dark UI themes.

The application automatically respects the user's operating system 
preferences on initial load while allowing manual override via a persistent 
navbar toggle.
